[project]
name = "pedalboard-gui"
version = "0.1.0"
description = "Gradio frontend GUI for Spotify Pedalboard"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradio>=6",
    "numpy",
    "pedalboard",
]
